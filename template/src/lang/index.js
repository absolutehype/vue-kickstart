const ch = require('./ch.js');
const en = require('./en.js');
const es = require('./es.js');
const fr = require('./fr.js');

const translationObj = { ch, en, es, fr };
// console.log(translationObj);

export default translationObj;
